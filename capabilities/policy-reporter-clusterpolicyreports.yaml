naftiko: 1.0.0-alpha2
info:
  label: Kyverno Policy Reporter API — ClusterPolicyReports
  description: 'Kyverno Policy Reporter API — ClusterPolicyReports. 1 operations. Lead operation: Kyverno List cluster-scoped
    policy reports. Self-contained Naftiko capability covering one Kyverno business surface.'
  tags:
  - Kyverno
  - ClusterPolicyReports
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    KYVERNO_API_KEY: KYVERNO_API_KEY
capability:
  consumes:
  - type: http
    namespace: policy-reporter-clusterpolicyreports
    baseUri: http://localhost:8080
    description: Kyverno Policy Reporter API — ClusterPolicyReports business capability. Self-contained, no shared references.
    resources:
    - name: api-v1-cluster-policy-reports
      path: /api/v1/cluster-policy-reports
      operations:
      - name: listclusterpolicyreports
        method: GET
        description: Kyverno List cluster-scoped policy reports
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: policy-reporter-clusterpolicyreports-rest
    port: 8080
    description: REST adapter for Kyverno Policy Reporter API — ClusterPolicyReports. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/v1/cluster-policy-reports
      name: api-v1-cluster-policy-reports
      description: REST surface for api-v1-cluster-policy-reports.
      operations:
      - method: GET
        name: listclusterpolicyreports
        description: Kyverno List cluster-scoped policy reports
        call: policy-reporter-clusterpolicyreports.listclusterpolicyreports
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: policy-reporter-clusterpolicyreports-mcp
    port: 9090
    transport: http
    description: MCP adapter for Kyverno Policy Reporter API — ClusterPolicyReports. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: kyverno-list-cluster-scoped-policy
      description: Kyverno List cluster-scoped policy reports
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: policy-reporter-clusterpolicyreports.listclusterpolicyreports
      outputParameters:
      - type: object
        mapping: $.
